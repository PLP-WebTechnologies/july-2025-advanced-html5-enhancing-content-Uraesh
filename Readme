Projet : Formulaire d'Inscription Étudiant - HTML5 Enrichi
Ce projet est une page web complète conçue pour répondre aux exigences d'un devoir sur le HTML5 avancé. Il démontre la maîtrise de la structuration de contenu riche et la création de formulaires web complexes, en mettant un accent particulier sur la sémantique et l'accessibilité.

🚀 Objectif
L'objectif principal de ce projet est de présenter une page unique qui intègre :
Du contenu organisé à l'aide de listes et de tableaux.
Des emplacements pour des médias (image, audio, vidéo) en utilisant des balises sémantiques.
Un formulaire HTML5 détaillé et multi-sections pour l'inscription d'un étudiant.
L'utilisation extensive des attributs de validation natifs d'HTML5.
Des bonnes pratiques d'accessibilité (ARIA, labels, etc.) pour garantir une expérience utilisateur inclusive.

✨ Fonctionnalités
La page est divisée en plusieurs sections thématiques :
Exigences du cours : Utilise des listes ordonnées (<ol>) et non ordonnées (<ul>) pour structurer les informations.
Suivi des étudiants : Présente des données sous forme de tableau (<table>) avec une structure sémantique correcte (<thead>, <tbody>, <caption>, scope).
Contenu média : Utilise des balises <figure> et <figcaption> pour intégrer des ressources visuelles, audio et vidéo (ici simulées par des blocs stylisés).
Formulaire d'inscription : Le cœur du projet. Ce formulaire est divisé en plusieurs <fieldset> pour une meilleure clarté et inclut une grande variété de champs de saisie (input) :
Types variés : text, email, tel, date, number, range, url, datetime-local, etc.
Validation native : required, minlength, maxlength, pattern, min, max.
Attributs d'aide : placeholder, autocomplete, title.
Accessibilité renforcée : Chaque champ est associé à un <label> et souvent décrit par des attributs aria-describedby pour lier des instructions supplémentaires.
Champs spécifiques : readonly, select, textarea, radio, checkbox.

🛠️ Comment l'utiliser
Aucune installation n'est requise. Il suffit de cloner ce projet et d'ouvrir le fichier enhanced-form.html (ou le nom que vous lui avez donné) directement dans un navigateur web moderne (Chrome, Firefox, Edge, etc.).

📝 Points à noter
Absence de CSS externe : Le style est minimal et principalement fonctionnel. L'objectif était de se concentrer sur la structure HTML.
Médias simulés : Pour des raisons de simplicité, les éléments <audio> et <video> n'ont pas de fichiers source (src). Les images sont également représentées par des blocs div stylisés.
Attribut novalidate : Le formulaire possède l'attribut novalidate. Cela désactive les bulles de validation par défaut du navigateur, ce qui est souvent fait lorsqu'on souhaite implémenter une validation personnalisée avec JavaScript. Cependant, tous les attributs de validation natifs sont bien présents dans le code et peuvent être utilisés par le navigateur si cet attribut est retiré.
